# 验证范围

2026-09-09，在 Ubuntu 22.04 / GNOME 42 / X11 / x86_64 的官方 ZCode 桌面上完成：

- 单次可见发送、等待、读取。174 字符中文任务完整回读后发送，约 19 秒返回，
  唯一 marker、中文确认和 12+8+5=25 均正确。
- 同一会话续聊两轮：不重述原清单即可恢复名称、时长和顺序，增加 6 分钟得到 31；
  再按上一版行号修改、删除和重排，得到 29 及差值 -2。两轮约 6 秒、11 秒，
  具有独立的 user message/turn ID，session ID 保持一致。
- 三个回合实际模型均为 builtin:bigmodel-coding-plan/GLM-5.3-Flash，工具调用为 0。
- 另一次三轮官方 CLI 实现测试通过文件和 review 传递上下文，没有被当作同一个 CLI 会话。

12ms 中文输入曾丢字，发送前发现；50ms 版本在上述实际任务中通过。
旧 ATK 的 GetActions 崩溃、窗口坐标偏移和高亮拦截点击已通过 Linux 补丁修复。
浅层焦点诊断仍可能误报，使用完整树和实际文本验收。

原始记录和截图含操作员环境信息，保留在原机器，不随公开插件打包。
历史成功不证明未来请求必然成功、免费，或长时间编码任务可靠。
本发布包的移植与离线检查见插件根目录 VALIDATION.md。
